namespace tests;

fun int add(int x, int y)
{
	return x + y;
}

fun void abc()
{
	let int a = add(1, 1);	// 2
	let int b = a + a;		// 4
	let int c = a + b;		// 6

	print(c);
}

fun void loop()
{
	for (let int i = 0; i < 10; i = i + 1)
	{
		print("welcome to zen!");
	}
}

fun void prints()
{
	let int val_int = 1;
	let float val_float = 1.0;
	let bool val_bool = false;

	println(val_int);
	println(val_float);
	println(val_bool);
}

enum Color
{
	Red,
	Orange,
	Yellow,
	Cyan
}

fun void enums()
{
	var Color color;
	color = Color::Red;
}