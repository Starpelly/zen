#load "entity.zen";

namespace game;

struct Crate
{
    Entity entity;
}

fun void crate_init(Crate* crate)
{
    // crate.entity.type = EntityType::Crate;
}

fun void crate_draw(Crate* crate)
{
    raylib::DrawRectangle(cast(int)(crate.entity.visualX * cast(float)CELL_WIDTH), cast(int)(crate.entity.visualY * cast(float)CELL_HEIGHT), CELL_WIDTH, CELL_HEIGHT, raylib::Blue);
}