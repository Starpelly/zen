// #load "tests.zen";
#load "fibonacci.zen";
// #load "windows.zen";
// #load "raylib.zen";

struct Vector2
{
	float x;
	float y;
}

struct Vector3
{
	float x;
	float y;
	float z;
}

enum Color
{
	Red,
	Orange,
	Yellow,
	Green,
	Blue,
	Purple,
}

fun void main()
{
	let int fib = math::fibonacci_recursive(10);
	// let int x = tests::add(1, 1);

	println(fib);
	
	var Vector2 vec;
	vec.x = 0.0;

	println("Hello");
}