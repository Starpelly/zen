#load "test.zen";
#load "fibonacci.zen";
// #load "windows.zen";
// #load "raylib.zen";

struct Vector2
{
	float x;
	float y;
}

fun void main()
{
	let int fib = math::fibonacci(10);
	let int x = tests::add(1, 1);

	println(fib);
}